# Use the Vite dev proxy so the browser calls same-origin `/api/...` (no CORS).
# Do not set VITE_API_BASE_URL to http://localhost:3000 — that triggers cross-origin requests.
# Optional: force a path prefix only:
# VITE_API_BASE_URL=/api/v1
